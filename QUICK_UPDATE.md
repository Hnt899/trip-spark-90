# Быстрое обновление данных для нового аккаунта

## Что нужно сделать

### 1. Получите данные из нового аккаунта МТС Exolve

**API ключ:**
- Войдите в личный кабинет МТС Exolve
- Перейдите в раздел "Ключи" или "API"
- Скопируйте API ключ

**Номер отправителя (опционально):**
- Если используете номер: Личный кабинет → "Нумерация" → "Мои номера"
- Если используете альфа-имя: Личный кабинет → "Сообщения" → "Альфа-имена"

### 2. Обновите секреты в Supabase

Выполните эти команды в терминале (замените значения на свои):

```bash
# Обновить API ключ (ОБЯЗАТЕЛЬНО)
npx supabase secrets set EXOLVE_API_KEY=ваш_новый_api_ключ

# Обновить номер отправителя (если используете номер)
npx supabase secrets set EXOLVE_SENDER=+79001234567

# ИЛИ обновить альфа-имя (если используете альфа-имя)
# npx supabase secrets set EXOLVE_SENDER=ваше_альфа_имя
```

### 3. Передеплойте Edge Function

```bash
npx supabase functions deploy send-sms-exolve
```

### 4. Проверьте работу

1. Попробуйте зарегистрироваться на сайте с номером телефона
2. Проверьте, что SMS приходит с вашим текстом (не тестовым)
3. Проверьте логи в Supabase Dashboard → Edge Functions → `send-sms-exolve` → Logs

## Готово! 🎉

После обновления данных и деплоя всё должно работать с новым аккаунтом.

