providers = ['node']

[phases.setup]
nixPkgs = ['nodejs_22', 'npm-10_x']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['echo "skip frontend build"']

[start]
cmd = 'node scripts/migrate.js && node server/index.js'
