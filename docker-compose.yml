services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    env_file:
      - .env.vps
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10

  app:
    build:
      context: .
      dockerfile: Dockerfile.app
    restart: unless-stopped
    env_file:
      - .env.vps
    environment:
      NODE_ENV: production
      PORT: 4000
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      RZD_FETCH_TIMEOUT_MS: ${RZD_FETCH_TIMEOUT_MS:-20000}
      RZD_MAX_RID_ATTEMPTS: ${RZD_MAX_RID_ATTEMPTS:-12}
      RZD_RID_DELAY_MS: ${RZD_RID_DELAY_MS:-900}
      RZD_HTTPS_PROXY: ${RZD_HTTPS_PROXY:-}
      RZD_CACHE_TTL_SEC: ${RZD_CACHE_TTL_SEC:-1800}
      RZD_DEMO_FALLBACK: ${RZD_DEMO_FALLBACK:-1}
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - uploads_data:/app/uploads
    expose:
      - "4000"

  web:
    build:
      context: .
      dockerfile: Dockerfile.web
    restart: unless-stopped
    depends_on:
      - app
    ports:
      - "80:80"

volumes:
  postgres_data:
  uploads_data:
