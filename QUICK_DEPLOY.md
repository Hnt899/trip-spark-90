# ⚡ Быстрый деплой на Vercel (5 минут)

## Шаг 1: Подготовка (1 минута)

Убедитесь, что код закоммичен в Git:

```bash
git add .
git commit -m "Ready for deployment"
git push
```

## Шаг 2: Регистрация на Vercel (1 минута)

1. Перейдите на https://vercel.com
2. Нажмите "Sign Up" и войдите через GitHub
3. Разрешите доступ к вашему репозиторию

## Шаг 3: Деплой (2 минуты)

1. Нажмите **"Add New Project"**
2. Выберите ваш репозиторий `trip-spark-90`
3. Vercel автоматически определит настройки:
   - Framework: **Vite**
   - Build Command: `npm run build`
   - Output Directory: `dist`
4. **ВАЖНО:** В разделе **"Environment Variables"** добавьте:

   ```
   VITE_SUPABASE_URL = https://fwjwqtwwsxchjlgwrmdj.supabase.co
   VITE_SUPABASE_ANON_KEY = ваш_anon_key_из_supabase
   ```

   Где взять `VITE_SUPABASE_ANON_KEY`:
   - Откройте Supabase Dashboard: https://supabase.com/dashboard
   - Выберите ваш проект
   - Settings → API
   - Скопируйте "anon public" ключ

5. Нажмите **"Deploy"**

## Шаг 4: Настройка CORS (1 минута)

После деплоя Vercel даст вам URL вида: `ваш-проект.vercel.app`

1. Откройте Supabase Dashboard
2. Settings → API
3. В разделе "CORS" добавьте ваш домен Vercel:
   ```
   https://ваш-проект.vercel.app
   ```
4. Нажмите "Save"

## Готово! 🎉

Сайт доступен по адресу: `https://ваш-проект.vercel.app`

---

## 📝 Что дальше?

- **Обновления:** При каждом push в GitHub сайт автоматически обновится
- **Логи:** Смотрите логи деплоя в Vercel Dashboard
- **Домен:** Можно подключить свой домен в настройках Vercel

## 🔧 Если что-то не работает:

1. Проверьте переменные окружения в Vercel
2. Проверьте CORS настройки в Supabase
3. Проверьте логи деплоя в Vercel Dashboard
4. Проверьте консоль браузера на ошибки

