# Trip Spark - Платформа для поиска и бронирования билетов

Веб-приложение для поиска и бронирования билетов на поезда, самолеты и автобусы.

## 🚀 Технологии

- **Frontend:** React + TypeScript + Vite
- **UI:** shadcn-ui + Tailwind CSS
- **Backend:** Supabase (Auth, Database, Edge Functions)
- **SMS:** МТС Exolve API

## 📦 Установка и запуск

```bash
# Клонируйте репозиторий
git clone <YOUR_GIT_URL>
cd trip-spark-90

# Установите зависимости
npm install

# Запустите сервер разработки
npm run dev
```

## 🔧 Настройка

1. Создайте файл `.env.local`:
```env
VITE_SUPABASE_URL=https://ваш-проект.supabase.co
VITE_SUPABASE_ANON_KEY=ваш_anon_key
```

2. Настройте Supabase:
   - Создайте проект на [supabase.com](https://supabase.com)
   - Выполните миграции из папки `supabase/migrations/`
   - Настройте Edge Functions (см. `EXOLVE_SETUP.md`)

## 📚 Документация

- **[EXOLVE_SETUP.md](./EXOLVE_SETUP.md)** - Настройка МТС Exolve для SMS

## 🛠️ Команды

```bash
# Разработка
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр сборки
npm run preview

# Линтинг
npm run lint
```

## 🤖 Локальный ИИ-чат-помощник

1. Создайте файл `.env` в корне проекта и добавьте:
```env
HF_API_TOKEN=ваш_token_hf
HF_MODEL=google/gemma-2b-it
PORT=4000
```

2. Установите зависимости (включая серверные):
```bash
npm install
```

3. Запустите сервер и фронтенд в разных терминалах:
```bash
npm run server   # стартует Express на http://localhost:4000
npm run dev      # стартует Vite-фронтенд
```

4. Чат обращается к endpoint `http://localhost:4000/api/support/chat` и использует модель из переменной `HF_MODEL`.
   Чтобы поменять модель, отредактируйте `HF_MODEL` в `.env` и перезапустите сервер.

## 📝 Лицензия

Private project
