# 📧 Настройка Email OTP в Supabase Dashboard

## Проблема

При регистрации через email приходит ссылка подтверждения вместо OTP кода.

## Решение: Настройка Email Templates в Supabase

### Шаг 1: Откройте настройки Email Templates

1. Перейдите в **Supabase Dashboard**: https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в **Settings** → **Authentication** → **Email Templates**

### Шаг 2: Настройте шаблон "Confirm signup"

1. Найдите шаблон **"Confirm signup"** (или **"Magic Link"**)
2. Нажмите **"Edit"** или создайте новый шаблон

### Шаг 3: Используйте этот шаблон для OTP кода

**Subject (Тема письма):**
```
Подтвердите регистрацию - ваш код: {{ .Token }}
```

**Body (Тело письма):**
```html
<h2>Подтвердите регистрацию</h2>

<p>Здравствуйте!</p>

<p>Ваш код подтверждения для регистрации:</p>

<div style="text-align: center; margin: 30px 0;">
  <h1 style="font-size: 36px; letter-spacing: 10px; font-weight: bold; color: #2563eb; margin: 20px 0;">
    {{ .Token }}
  </h1>
</div>

<p>Введите этот код в форме подтверждения.</p>

<p style="color: #666; font-size: 14px;">
  Код действителен в течение 10 минут.
</p>

<p style="color: #666; font-size: 14px; margin-top: 30px;">
  Если вы не регистрировались на нашем сайте, просто проигнорируйте это письмо.
</p>
```

**Важно:** 
- Используйте переменную `{{ .Token }}` для отображения OTP кода
- Не используйте `{{ .ConfirmationURL }}` - это для Magic Link

### Шаг 4: Настройте шаблон "Magic Link" (если используется)

Если Supabase все еще отправляет Magic Link, настройте и этот шаблон:

1. Найдите шаблон **"Magic Link"**
2. Измените его, чтобы он тоже показывал код:

```html
<h2>Вход в систему</h2>

<p>Ваш код для входа:</p>

<h1 style="font-size: 32px; letter-spacing: 8px; text-align: center;">
  {{ .Token }}
</h1>

<p>Или перейдите по ссылке: <a href="{{ .ConfirmationURL }}">Войти</a></p>
```

### Шаг 5: Проверьте настройки Auth

1. Перейдите в **Settings** → **Authentication** → **Auth Settings**
2. Убедитесь, что:
   - ✅ **"Enable email confirmations"** - включен
   - ✅ **"Enable email change confirmations"** - включен (опционально)
   - ✅ Email provider настроен правильно

### Шаг 6: Проверьте настройки Site URL

1. Перейдите в **Settings** → **Authentication** → **URL Configuration**
2. Убедитесь, что:
   - **Site URL** указан правильно
   - **Redirect URLs** содержит ваш домен

**Важно:** Если `emailRedirectTo` не указан в коде, Supabase должен отправлять OTP код, а не ссылку.

---

## Альтернативный способ: Отключить Magic Link

Если настройка шаблонов не помогает:

1. **Settings** → **Authentication** → **Auth Settings**
2. Найдите настройку **"Enable email confirmations"**
3. Убедитесь, что она включена
4. В коде убедитесь, что **не указан** `emailRedirectTo`:

```typescript
const { error } = await supabase.auth.signInWithOtp({
  email: emailOrPhone,
  options: {
    shouldCreateUser: true,
    // НЕ указываем emailRedirectTo - это заставит отправить OTP код
  }
});
```

---

## Проверка

После настройки:

1. Попробуйте зарегистрироваться через email
2. Проверьте письмо - должен прийти код, а не ссылка
3. Проверьте, что код работает при вводе

---

## Если все еще приходит ссылка

1. **Проверьте логи:**
   - Supabase Dashboard → Logs → Auth Logs
   - Посмотрите, какой тип OTP отправляется

2. **Проверьте код:**
   - Убедитесь, что в `signInWithOtp` не указан `emailRedirectTo`
   - Убедитесь, что используется `signInWithOtp`, а не `signInWithPassword`

3. **Проверьте шаблоны:**
   - Убедитесь, что в шаблоне используется `{{ .Token }}`
   - Убедитесь, что шаблон сохранен и активен

---

## Дополнительная информация

- [Supabase Email Templates](https://supabase.com/docs/guides/auth/auth-email-templates)
- [Supabase OTP Auth](https://supabase.com/docs/guides/auth/auth-otp)

