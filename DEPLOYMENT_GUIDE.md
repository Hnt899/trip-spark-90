# 🚀 Руководство по деплою сайта для демонстрации заказчику

## Варианты деплоя (от простого к сложному)

---

## 🥇 Вариант 1: Vercel (РЕКОМЕНДУЕТСЯ - самый простой)

**Преимущества:**
- ✅ Бесплатный тариф
- ✅ Автоматический деплой из GitHub
- ✅ Автоматический HTTPS
- ✅ Быстрый CDN
- ✅ Простая настройка переменных окружения
- ✅ Бесплатный домен вида `ваш-проект.vercel.app`

### Шаги:

1. **Подготовка кода:**
   ```bash
   # Убедитесь, что код закоммичен в Git
   git add .
   git commit -m "Prepare for deployment"
   ```

2. **Создайте аккаунт на Vercel:**
   - Перейдите на https://vercel.com
   - Войдите через GitHub (рекомендуется)

3. **Деплой:**
   - Нажмите "Add New Project"
   - Выберите ваш репозиторий
   - Vercel автоматически определит Vite проект
   - Настройки:
     - **Framework Preset:** Vite
     - **Build Command:** `npm run build`
     - **Output Directory:** `dist`
     - **Install Command:** `npm install`

4. **Настройте переменные окружения:**
   В разделе "Environment Variables" добавьте:
   ```
   VITE_SUPABASE_URL=ваш_supabase_url
   VITE_SUPABASE_ANON_KEY=ваш_supabase_anon_key
   ```

5. **Деплой:**
   - Нажмите "Deploy"
   - Через 1-2 минуты сайт будет доступен по адресу `ваш-проект.vercel.app`

6. **Обновление:**
   - При каждом push в GitHub сайт автоматически обновится

---

## 🥈 Вариант 2: Netlify

**Преимущества:**
- ✅ Бесплатный тариф
- ✅ Автоматический деплой из GitHub
- ✅ Простая настройка
- ✅ Бесплатный домен `ваш-проект.netlify.app`

### Шаги:

1. **Создайте файл `netlify.toml` в корне проекта:**
   ```toml
   [build]
     command = "npm run build"
     publish = "dist"
   
   [[redirects]]
     from = "/*"
     to = "/index.html"
     status = 200
   ```

2. **Создайте аккаунт на Netlify:**
   - Перейдите на https://netlify.com
   - Войдите через GitHub

3. **Деплой:**
   - Нажмите "Add new site" → "Import an existing project"
   - Выберите репозиторий
   - Настройки:
     - **Build command:** `npm run build`
     - **Publish directory:** `dist`

4. **Настройте переменные окружения:**
   В разделе "Site settings" → "Environment variables":
   ```
   VITE_SUPABASE_URL=ваш_supabase_url
   VITE_SUPABASE_ANON_KEY=ваш_supabase_anon_key
   ```

5. **Деплой:**
   - Нажмите "Deploy site"
   - Сайт будет доступен по адресу `ваш-проект.netlify.app`

---

## 🥉 Вариант 3: Cloudflare Pages

**Преимущества:**
- ✅ Бесплатный тариф
- ✅ Очень быстрый CDN
- ✅ Автоматический деплой из GitHub
- ✅ Бесплатный домен `ваш-проект.pages.dev`

### Шаги:

1. **Создайте аккаунт на Cloudflare:**
   - Перейдите на https://pages.cloudflare.com
   - Войдите через GitHub

2. **Деплой:**
   - Нажмите "Create a project"
   - Выберите репозиторий
   - Настройки:
     - **Framework preset:** Vite
     - **Build command:** `npm run build`
     - **Build output directory:** `dist`

3. **Настройте переменные окружения:**
   В разделе "Settings" → "Environment variables":
   ```
   VITE_SUPABASE_URL=ваш_supabase_url
   VITE_SUPABASE_ANON_KEY=ваш_supabase_anon_key
   ```

4. **Деплой:**
   - Нажмите "Save and Deploy"
   - Сайт будет доступен по адресу `ваш-проект.pages.dev`

---

## 📋 Вариант 4: GitHub Pages (бесплатно, но сложнее)

**Преимущества:**
- ✅ Полностью бесплатно
- ✅ Интеграция с GitHub

**Недостатки:**
- ❌ Нужно настроить GitHub Actions
- ❌ Нет автоматической настройки переменных окружения (нужно использовать публичные переменные или другой подход)

### Шаги:

1. **Создайте файл `.github/workflows/deploy.yml`:**
   ```yaml
   name: Deploy to GitHub Pages
   
   on:
     push:
       branches: [ main ]
   
   jobs:
     build-and-deploy:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v3
         
         - name: Setup Node.js
           uses: actions/setup-node@v3
           with:
             node-version: '18'
             cache: 'npm'
         
         - name: Install dependencies
           run: npm ci
         
         - name: Build
           run: npm run build
           env:
             VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
             VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
         
         - name: Deploy
           uses: peaceiris/actions-gh-pages@v3
           with:
             github_token: ${{ secrets.GITHUB_TOKEN }}
             publish_dir: ./dist
   ```

2. **Настройте секреты в GitHub:**
   - Перейдите в Settings → Secrets and variables → Actions
   - Добавьте:
     - `VITE_SUPABASE_URL`
     - `VITE_SUPABASE_ANON_KEY`

3. **Включите GitHub Pages:**
   - Settings → Pages
   - Source: `gh-pages` branch
   - Сайт будет доступен по адресу `ваш-username.github.io/ваш-репозиторий`

---

## 🔧 Общие шаги для всех вариантов:

### 1. Получите данные Supabase:

```bash
# Ваш Supabase URL (из .env.local или Supabase Dashboard)
VITE_SUPABASE_URL=https://fwjwqtwwsxchjlgwrmdj.supabase.co

# Ваш Supabase Anon Key (из .env.local или Supabase Dashboard)
VITE_SUPABASE_ANON_KEY=ваш_anon_key
```

### 2. Проверьте build локально:

```bash
# Установите зависимости
npm install

# Соберите проект
npm run build

# Проверьте, что dist папка создалась
ls dist
```

### 3. Проверьте, что все работает:

```bash
# Запустите preview
npm run preview
```

---

## 🎯 Рекомендация для быстрого деплоя:

**Используйте Vercel** - это самый быстрый и простой способ:

1. Зарегистрируйтесь на vercel.com через GitHub
2. Нажмите "Add New Project"
3. Выберите репозиторий
4. Добавьте переменные окружения
5. Нажмите "Deploy"
6. Готово! Сайт доступен через 1-2 минуты

---

## 🔒 Важные моменты:

1. **Переменные окружения:**
   - Никогда не коммитьте `.env.local` в Git
   - Всегда используйте переменные окружения на платформе деплоя

2. **API ключи:**
   - `VITE_SUPABASE_ANON_KEY` - это публичный ключ, его можно использовать в фронтенде
   - `EXOLVE_API_KEY` - это секрет, он используется только в Edge Functions (уже настроен в Supabase)

3. **CORS:**
   - Убедитесь, что в Supabase Dashboard → Settings → API добавлен домен вашего деплоя в "Allowed Origins"

4. **Edge Functions:**
   - Edge Functions уже деплоятся вместе с Supabase
   - Они доступны по адресу `https://ваш-проект.supabase.co/functions/v1/...`

---

## 📱 После деплоя:

1. Проверьте, что сайт открывается
2. Проверьте авторизацию (отправка SMS)
3. Проверьте все основные функции
4. Отправьте ссылку заказчику

---

## 🆘 Если что-то не работает:

1. **Проверьте логи деплоя** на платформе
2. **Проверьте переменные окружения** - они должны быть установлены
3. **Проверьте CORS настройки** в Supabase
4. **Проверьте консоль браузера** на ошибки

---

## 💡 Дополнительные возможности:

- **Кастомный домен:** Все платформы поддерживают подключение своего домена
- **Preview деплои:** Vercel и Netlify создают preview для каждого PR
- **Аналитика:** Можно подключить Google Analytics или другие сервисы

