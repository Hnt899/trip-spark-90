# === Railway Environment Variables ===

# PostgreSQL (Railway даёт автоматически, если подключить Postgres-плагин)
DATABASE_URL=postgresql://user:password@host:5432/railway

# JWT secret (сгенерируйте длинную случайную строку)
JWT_SECRET=your-random-secret-here

# HuggingFace AI Chat (необязательно)
HF_API_TOKEN=hf_...
HF_MODEL=meta-llama/Meta-Llama-3-8B-Instruct

# CORS: домены через запятую (ваш Vercel-домен)
ALLOWED_ORIGINS=https://your-project.vercel.app

# Оплата: Stripe Checkout (секрет только на сервере; ключи из Dashboard — test/live)
# Локально: положите ключи в `.env.local` (подхватывается server/db.js и Vite) или в `.env`
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...   # после `npm run stripe:listen` скопируйте Ready! whsec_…
# Если «stripe не является командой» — установите CLI: winget install Stripe.StripeCLI (новый терминал)
# VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...   # опционально, если понадобится Stripe.js на фронте

# SKIP_WEBPAY=true   # без оплаты: сразу paid (только разработка)

# Порт (Railway задаёт сам, не нужно менять)
# PORT=4000
